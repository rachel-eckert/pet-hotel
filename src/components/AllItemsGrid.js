import { Grid } from "@mui/material";
import { styled } from "@mui/system";

const StyledAllGrid = styled(Grid)({
  float: "center",
  justifyContent: "center",
  backgroundColor: "#87BBA2",
  paddingTop: 40,
  height: "100vh",
});

export default StyledAllGrid;
